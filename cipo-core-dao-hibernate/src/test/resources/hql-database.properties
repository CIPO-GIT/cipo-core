# TODO - Add this file on the external classpath.

# Information related to connection - Testing mode only. 
db.driver=org.hsqldb.jdbcDriver
db.url=jdbc:hsqldb:mem:testdb
db.username=root
db.password=

# Information related to persistence - JPA & Hibernate.
db.dialect=org.hibernate.dialect.HSQLDialect
db.show_sql=true
db.format_sql=true
